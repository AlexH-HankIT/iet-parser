<?php

namespace MrCrankHank\IetParser\tests;

use MrCrankHank\IetParser\Exceptions\DuplicationErrorException;
use MrCrankHank\IetParser\Parser\GlobalOptionParser;
use League\Flysystem\Adapter\Local;
use League\Flysystem\Filesystem;
use PHPUnit_Framework_TestCase;

/**
 * Class Normalizer
 * @package MrCrankHank\IetParser\tests
 */
class Normalizer extends PHPUnit_Framework_TestCase {
    /**
     * Create a new normalizer object
     * Then call the diff function and
     * check if the output equals the expected one
     */
   public function testDiffMethod()
   {

   }

    /**
     * Create a new normalizer object
     * Then call the check method and see
     * if the result is expected
     */
   public function testCheckMethod()
   {

   }

    /**
     * Create a new normalizer object
     * Then call the write method and see
     * if the written file equals is expected
     */
   public function testWriteMethod()
   {

   }
}